/// Onboarding — Provider (reserved)
///
/// State management for the onboarding feature is currently handled
/// directly in [OnboardingScreen] via local [StatefulWidget] state
/// since the logic is simple (page index + completion flag).
///
/// This file is reserved for future use if the onboarding flow
/// grows in complexity (e.g. dynamic content, A/B testing).
library;
